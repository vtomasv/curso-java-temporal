package com.sigeo.clase09;

import io.temporal.client.WorkflowClient;
import io.temporal.serviceclient.WorkflowServiceStubs;
import io.temporal.worker.Worker;
import io.temporal.worker.WorkerFactory;

public class ReinicioWorker {

    public static void main(String[] args) {
        // TODO(C09-E05): Configurar y arrancar el Worker para probar reinicios
        // 1. Configurar conexión a Temporal local
        // 2. Registrar RevisionWorkflowImpl
        // 3. Iniciar el worker
        // Instrucciones: Ejecutar este main, iniciar un workflow desde CLI,
        // detener este proceso (Ctrl+C) mientras el workflow duerme, y volver a iniciarlo.
        
        throw new UnsupportedOperationException("TODO C09-E05");
    }
}
